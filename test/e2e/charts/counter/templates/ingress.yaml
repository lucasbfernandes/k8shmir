apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: counter-app-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: counter-app
                port:
                  number: 3000
